#include <bits/stdc++.h>
#define ll long long
using namespace std;
void solve(ll ar[])
{
    ll m,d,a,s;

    if(ar[3]-ar[2]==1 || )
}
int main() {
	int t;
	cin>>t;
	while(t-->0)
	{
	    ll ar[4];
	    for(int i=0;i<4;i++)
	    cin>>ar[i];
	    sort(ar.begin(),ar.end());
	}
	return 0;
}
